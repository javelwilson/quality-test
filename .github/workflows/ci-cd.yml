# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Continuous Integration and Delivery
on:
  push:
    branches: [main, staging, develop]
  pull_request:
    branches: [main, staging, develop]

jobs:
  test_and_build:
     runs-on: ubuntu-latest
     steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Run npm install, test and build
        run: |
          npm ci
          npm run test
          npm run build
      - name: Upload build artifact
        uses: actions/upload-artifact@v2
        with:
          name: build
          path: build
          retention-days: 1

  deploy_to_production:
    if: github.ref == 'refs/heads/main'
    needs: test_and_build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Download build artifact
        uses: actions/download-artifact@v2
        with:
          name: build
          path: build
      - name: Deploy to firebase hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: >-
            ${{ secrets.FIREBASE_SERVICE_ACCOUNT_QUALITY_TEST_PRODUCTION_FD975
            }}
          channelId: live
          projectId: quality-test-production-fd975
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels

  deploy_to_staging:
    if: github.ref == 'refs/heads/staging'
    needs: test_and_build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Download build artifact
        uses: actions/download-artifact@v2
        with:
          name: build
          path: build
      - name: Deploy to firebase hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: >-
            ${{ secrets.FIREBASE_SERVICE_ACCOUNT_QUALITY_TEST_STAGING
            }}
          channelId: live
          projectId: quality-test-staging
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels

  deploy_to_development:
    if: github.ref == 'refs/heads/develop'
    needs: test_and_build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Download build artifact
        uses: actions/download-artifact@v2
        with:
          name: build
          path: build
      - name: Deploy to firebase hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: >-
            ${{ secrets.FIREBASE_SERVICE_ACCOUNT_QUALITY_TEST_DEVELOPMENT
            }}
          channelId: live
          projectId: quality-test-development
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels



